# OS / editor cruft
.DS_Store
*~

# Logs
*.log

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.pytest_cache/

# Build / dist artifacts (PyInstaller)
build/
dist/
*.spec

# Local / generated data outputs
# NOTE: bundled synthetic demo data in files/ is intentionally tracked (PRD N12).
# Only local/generated outputs are ignored.
data/local/
data/raw/
data/live_*.xlsx
*.local.xlsx
*.local.csv

# Claude Code local settings / session state (never commit)
.claude/

# Internal planning docs / specs — kept locally, intentionally untracked
/PRD.md
/TECH_SPEC.md
/LAMC_scheduling_data_spec.md
/docs/
