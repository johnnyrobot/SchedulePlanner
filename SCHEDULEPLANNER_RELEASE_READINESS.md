All ground truth verified, plus one important nuance: the v0.2.0-push CI "failure" was a GitHub billing/spending-limit block, not a code failure — and HEAD CI is genuinely green (6/6). The release-build.yml is workflow_dispatch-only with correct E5 permissions, and all sign/notarize/package scripts exist. I have what I need.

# SchedulePlanner — Release-Readiness Audit (main bfe9b78)

## 1. Verdict: **READY AFTER MECHANICS**

The **code** is ship-ready and that conclusion is strongly supported: main HEAD (`bfe9b78`) is CI-green on all 6 required jobs (run `27561273257`, 2026-06-15), the full 1046-test offline suite passes, the no-copyleft license gate verifies all 41 SBOM components, the determinism golden matches, and the prior 4-pillar ship review on this stack returned SHIP (0 blocker / 0 major). The 7-commit unreleased delta (PR #85) is low-risk by construction — 2 robustness fixes (`program_mapper` named-error, `generate_sbom` license-vetting) plus 40 new offline hardening tests, none of which touch engine logic. **Release mechanics, not code, are what's missing.** There are **0 blockers and 0 majors in the engineering sense** — but three mechanical facts gate an actual ship: (a) `release-build.yml` has **never executed on a runner**, so the signed/notarized/SLSA/SBOM artifact has never been produced by the pipeline; (b) **no GitHub Release artifact exists for v0.2.0** (latest published is still v0.1.0 from 2026-06-03); and (c) the `VERSION` file still reads `0.2.0` even though main is 7 commits past that tag. These are procedural steps, each with a concrete fix — hence READY AFTER MECHANICS, not NOT READY.

> **One caveat worth flagging before dispatch:** the v0.2.0 *push* CI run (`27529858006`) shows red, but the annotations confirm this was a **GitHub billing / spending-limit block** ("recent account payments have failed"), not a code or test failure — every job was *not started*, not failed. Resolve the GitHub Actions billing状态 before dispatching `release-build.yml`, or the macOS runner job will be blocked the same way.

## 2. The decision that matters: **cut a NEW v0.2.1 from current main — do NOT retroactively ship v0.2.0**

Recommendation: **(b) cut v0.2.1 from `bfe9b78`.** The v0.2.0 tag (`3cf1540`) was never published as an artifact, and current main is 7 commits ahead with **two production fixes that are compiled into the bundle** (`sources/program_mapper.py` named `SourceDataError`; `scripts/generate_sbom.py` license-gate hardening that closes a copyleft-bypass hole). Shipping the never-published v0.2.0 tag would knowingly ship without those fixes; retagging v0.2.0 onto `bfe9b78` would violate tag immutability. The clean path is a new patch tag (`v0.2.1`) that captures the hardening delta. Bump `VERSION` to `0.2.1` first (the bundle's `Info.plist` version string is stamped from this file by `build_macos.sh`), then tag.

## 3. Per-area summary

| Area | Ready | Blocker/Major items | Fix |
|---|---|---|---|
| **Release-build pipeline** (`release-build.yml`) | ✅ structurally | None (all info-level). Workflow is configured correctly: `workflow_dispatch`, `id-token: write` + `attestations: write` declared (lines 38–39) for E5 SLSA via `attest-build-provenance@v2`, scripts syntax-clean, deps hash-pinned, JRE fetch idempotent. | None required to be *correct* — but see versioning/changelog: it has **never run on a runner**, so first dispatch is itself a validation step. |
| **v0.2.0 publish gap** | ❌ | *(minor)* No GitHub Release ever cut for v0.2.0; `VERSION` is stale at `0.2.0` while main has 2 post-tag production fixes. | Don't publish v0.2.0. Bump `VERSION`→`0.2.1`, tag `v0.2.1`, publish from `bfe9b78`. |
| **Delta risk** (v0.2.0..main, PR #85) | ✅ | None. 2 source fixes are backwards-compatible (error-naming is non-functional on the happy path; SBOM is build-time-only, never imported by the app); 40 new offline tests, zero regressions; determinism preserved (`test_determinism_e2e.py`). | None. |
| **Versioning / changelog / tag hygiene** | ❌ | *(minor)* `VERSION` stale at `0.2.0`. *(minor)* No `CHANGELOG.md`/release-notes mechanism — notes are ad hoc in tag annotations / Release bodies. *(info)* `release-build.yml` never validated on a runner. | Bump `VERSION` to `0.2.1` + commit + tag `v0.2.1`. Optionally add `CHANGELOG.md` (keepachangelog format). Do one manual `release-build.yml` dispatch to validate before wiring to tags. |
| **Code / CI / determinism** | ✅ | None. CI all-green 6/6 on `bfe9b78`; 1046 tests pass; license gate clean (41 components); determinism golden matches. | None. |

**Net:** 0 blockers, 0 majors, 2 minors (stale `VERSION`; missing CHANGELOG). 5 serious candidate findings were raised and refuted during adversarial review. 3/5 areas fully ready; the 2 "not ready" areas are both pure release-mechanics, both fixed by the checklist below.

## 4. Ordered ship checklist

1. **Unblock GitHub Actions billing.** Resolve the spending-limit/payment failure that no-started the v0.2.0-push CI run (`27529858006`); otherwise the `macos-latest` runner job in `release-build.yml` will be blocked identically. Confirm a fresh `main` CI run goes green end-to-end.
2. **Bump the version.** On a short branch: `echo 0.2.1 > VERSION` → commit `chore(release): bump version 0.2.0 -> 0.2.1` → merge to `main`. (Optional but recommended: add/update `CHANGELOG.md` with the PR #85 hardening delta in the same commit.)
3. **Tag the release.** `git tag -a v0.2.1 -m "SchedulePlanner v0.2.1 — ship-review hardening (PR #85)"` on the new `main` HEAD; `git push origin v0.2.1`.
4. **Dispatch `release-build.yml` (first-ever run).** GitHub Actions UI → `release-build.yml` → Run workflow → target the `v0.2.1` tag. This is simultaneously the build *and* the workflow's one-time runner validation. It runs `fetch_jre.sh`, `build_macos.sh`, `verify_macos_build.sh`, generates the **E16 SBOM**, and emits the **E5 SLSA build-provenance attestation** via `attest-build-provenance@v2`.
5. **Sign + notarize + staple.** Run `scripts/sign_notarize_macos.sh` (Developer ID "Quang Phung (7XU3QW326W)", notary profile `schedplanner-notary`) on the built `.app`; package via `scripts/package_release.sh` to produce the signed/notarized/stapled `.dmg`/`.zip`. (Signing/notarization is out of `release-build.yml`'s scope — it produces the unsigned bundle + SBOM + attestation.)
6. **Verify the artifact.** Confirm: `verify_macos_build.sh` PASS (Mach-O, bundled Temurin JRE, OpenDataLoader jar, no-bloat guard); `spctl --assess` / `stapler validate` accept the notarized bundle; the **SLSA attestation** verifies against the tag; the **SBOM** license gate shows no GPL/AGPL; and the app launches (demo + live build, Gemma detected). Sanity-check the bundle's `Info.plist` reports `0.2.1`, not `0.2.0`.
7. **Publish the GitHub Release.** `gh release create v0.2.1` on `johnnyrobot/SchedulePlanner` with the signed/notarized/stapled `.dmg` (and `.zip`) + the SBOM and SLSA attestation attached; release body = the CHANGELOG/PR #85 notes. This closes the publish gap (latest published moves from v0.1.0 → v0.2.1).
8. **Follow-up (next cycle, non-blocking):** wire `release-build.yml` to `push: tags: 'v*'` now that it's runner-validated; upgrade `checkout`/`setup-python`/`upload-artifact` actions ahead of GitHub's Node 24 default (2026-06-16).
